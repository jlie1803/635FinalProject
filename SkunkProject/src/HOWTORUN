In Package Explorer, find src folder. Inside src folder, find SkunkApp class which has the main method and should be run by right clicking Run As Java application. 

In Package Explorer, find test folder. Right click on the folder and choose Run As JUnit Test.